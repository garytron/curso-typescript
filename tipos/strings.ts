(() => {
    const spiderman: string = 'Spiderman';
    const carnage: string = "Carnage";
    const venom: string = `Venom`;

    console.log(` I'm ${ spiderman }`);
    console.log( spiderman.toUpperCase() );
    
})